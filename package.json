{
  "name": "riddle-web",
  "version": "3.2.0",
  "description": "Tom Riddle's diary for the web — faithful port of MaximeRivest/Riddle",
  "scripts": {
    "build": "npm install",
    "deploy": "wrangler deploy",
    "deploy:pages": "wrangler pages deploy ./dist --project-name=riddle-web"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "@cloudflare/workers-types": "^0.20240712.0"
  }
}
