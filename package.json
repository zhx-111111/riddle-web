{
  "name": "tom-riddles-diary",
  "version": "4.0.0",
  "description": "Tom Riddle's Diary — a sentient AI diary that writes back in ink on Cloudflare Workers",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "echo 'No build step needed'"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
